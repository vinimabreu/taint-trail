name: Before the fix
on:
  issue_comment:
    types: [created]
jobs:
  helper:
    if: contains(github.event.comment.body, '/helper')
    runs-on: ubuntu-latest
    steps:
      - name: Run the helper with the comment
        run: helper-cli --prompt "${{ github.event.comment.body }}"
