name: Documented gap, awk system()
on:
  issue_comment:
    types: [created]
jobs:
  gap:
    runs-on: ubuntu-latest
    steps:
      - env:
          BODY: ${{ github.event.comment.body }}
        run: awk -v b="$BODY" 'BEGIN{system("echo " b)}'
