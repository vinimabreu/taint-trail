name: Single line, stderr merged then appended
on:
  issue_comment:
    types: [created]
jobs:
  write:
    runs-on: ubuntu-latest
    steps:
      - id: parse
        env:
          BODY: ${{ github.event.comment.body }}
        run: |
          echo "x=$BODY" 2>&1 >> "$GITHUB_OUTPUT"
