on: issue_comment
jobs:
  j:
    runs-on: ubuntu-latest
    env:
      BODY: ${{ github.event.comment.body }}
    steps:
      - run: |
          cat <<EOF | python3 parse.py
          $BODY
          EOF
