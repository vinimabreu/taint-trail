name: php -r names the variable
on:
  issue_comment:
    types: [created]
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - id: s
        env:
          BODY: ${{ github.event.comment.body }}
        run: php -r "system(getenv(\"BODY\"));"
