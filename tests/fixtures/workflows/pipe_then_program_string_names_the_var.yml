on: issue_comment
jobs:
  j:
    runs-on: ubuntu-latest
    env:
      BODY: ${{ github.event.comment.body }}
    steps:
      - run: echo "$BODY" | python3 -c 'import os; os.system(os.environ["BODY"])'
