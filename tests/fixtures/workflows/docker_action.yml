name: Docker action
on:
  pull_request_target:
    types: [opened]
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - uses: example/docker-action@v1
        with:
          text: ${{ github.event.pull_request.title }}
