name: runs-on label list with windows
on:
  issue_comment:
    types: [created]
jobs:
  win:
    runs-on: [self-hosted, windows]
    steps:
      - env:
          BODY: ${{ github.event.comment.body }}
        run: Invoke-Expression $env:BODY
