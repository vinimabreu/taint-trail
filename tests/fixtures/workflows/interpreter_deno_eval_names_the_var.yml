name: deno eval names the variable
on:
  issue_comment:
    types: [created]
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - id: s
        env:
          BODY: ${{ github.event.comment.body }}
        run: deno eval "console.log(Deno.env.get(\"BODY\"))"
